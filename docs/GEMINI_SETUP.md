# 🤖 การติดตั้ง Google Gemini API สำหรับ Chatbot

## 📋 ขั้นตอนการติดตั้ง

### 1. สร้าง Google Gemini API Key

1. ไปที่ [Google AI Studio](https://makersuite.google.com/app/apikey)
2. เข้าสู่ระบบด้วย Google Account
3. คลิก **"Get API Key"** หรือ **"Create API Key"**
4. เลือก Project ที่ต้องการ (หรือสร้าง Project ใหม่)
5. คัดลอก API Key ที่ได้

### 2. เพิ่ม API Key ลงในโค้ด

เปิดไฟล์ `js/components/chatbot.js` และแก้ไขบรรทัดที่ 6:

```javascript
const GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY_HERE'; // แทนที่ด้วย API Key ของคุณ
```

เปลี่ยนเป็น:

```javascript
const GEMINI_API_KEY = 'AIzaSy...'; // ใส่ API Key ที่คัดลอกมา
```

### 3. ทดสอบ Chatbot

1. เปิดไฟล์ `chatbot.html` ในเบราว์เซอร์
2. ลองพิมพ์คำถาม เช่น "ทำนายดวงความรักของฉันหน่อย"
3. รอ AI ตอบกลับ (ประมาณ 2-5 วินาที)

## 🔧 การตั้งค่าเพิ่มเติม

### ปรับแต่งความสร้างสรรค์ของ AI

ในไฟล์ `js/components/chatbot.js` หาส่วน `generationConfig`:

```javascript
generationConfig: {
    temperature: 0.9,      // ความสร้างสรรค์ (0.0-1.0) สูง = สร้างสรรค์มากขึ้น
    topK: 40,              // จำนวนตัวเลือกคำถัดไป
    topP: 0.95,            // ความหลากหลายของคำตอบ
    maxOutputTokens: 1024, // ความยาวสูงสุดของคำตอบ
}
```

### ปรับแต่ง System Prompt

แก้ไข `systemPrompt` ในฟังก์ชัน `getGeminiResponse()` เพื่อเปลี่ยนบุคลิกของ AI:

```javascript
const systemPrompt = `คุณคือ AI ผู้นำทางจิตวิญญาณ...`;
```

## 💰 ข้อมูลราคา

### Free Tier (ฟรี)
- **60 คำขอต่อนาที**
- **1,500 คำขอต่อวัน**
- เพียงพอสำหรับการพัฒนาและเว็บไซต์ขนาดเล็ก

### ข้อจำกัดของ Free Tier
- หากใช้เกิน quota จะได้รับ error
- ใช้ได้กับ Gemini Pro model

## 🚨 ข้อควรระวัง

### ความปลอดภัยของ API Key

**❌ อย่าทำ:**
- อย่าเผยแพร่ API Key บน GitHub หรือที่สาธารณะ
- อย่าฝัง API Key โดยตรงในโค้ดที่ deploy

**✅ ควรทำ:**
- ใช้ Environment Variables
- สร้าง Backend Server เพื่อซ่อน API Key
- ใช้ Firebase Functions หรือ Cloudflare Workers

### วิธีซ่อน API Key (แนะนำ)

#### วิธีที่ 1: ใช้ Environment Variables (สำหรับ Local Development)

สร้างไฟล์ `.env`:
```
GEMINI_API_KEY=AIzaSy...
```

เพิ่มใน `.gitignore`:
```
.env
```

#### วิธีที่ 2: สร้าง Backend Server (สำหรับ Production)

สร้าง Simple Server ด้วย Node.js:

```javascript
// server.js
const express = require('express');
const app = express();

app.post('/api/chat', async (req, res) => {
    const userMessage = req.body.message;
    
    // เรียก Gemini API ที่นี่
    const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=${process.env.GEMINI_API_KEY}`, {
        // ... API call
    });
    
    res.json(await response.json());
});

app.listen(3000);
```

แล้วเปลี่ยน Frontend ให้เรียก `/api/chat` แทน Gemini API โดยตรง

## 🎨 ฟีเจอร์ที่เพิ่มเข้ามา

### ✅ AI แท้จริง
- ใช้ Google Gemini Pro model
- ตอบคำถามได้หลากหลายและชาญฉลาด
- เข้าใจบริบทของการสนทนา

### ✅ Personality ที่กำหนดเอง
- AI จะตอบในฐานะผู้นำทางจิตวิญญาณ
- ใช้ภาษาไทยที่สวยงามและลึกลับ
- ให้คำทำนายที่มีความหวังและสร้างแรงบันดาลใจ

### ✅ Error Handling
- มี Fallback responses เมื่อ API มีปัญหา
- แสดงข้อความแจ้งเตือนที่เป็นมิตร
- บันทึก error ไว้ใน console

### ✅ Safety Settings
- กรองเนื้อหาที่ไม่เหมาะสม
- ป้องกันการใช้งานในทางที่ผิด

## 📊 การตรวจสอบการใช้งาน

ดู quota การใช้งานได้ที่: [Google AI Studio - API Keys](https://makersuite.google.com/app/apikey)

## 🔍 การแก้ไขปัญหา

### ❌ Error: API Key invalid
- ตรวจสอบว่าคัดลอก API Key ถูกต้อง
- ตรวจสอบว่า API key ยังไม่หมดอายุ

### ❌ Error: 429 (Too Many Requests)
- คุณใช้งานเกิน quota
- รอ 1 นาทีแล้วลองใหม่

### ❌ Error: CORS
- ไม่สามารถเรียก API โดยตรงจาก `file://`
- ต้องใช้ Local Server เช่น Live Server extension

### 🚀 วิธีเปิด Local Server

#### ใช้ VS Code Live Server:
1. ติดตั้ง Extension "Live Server"
2. คลิกขวาที่ `chatbot.html`
3. เลือก "Open with Live Server"

#### หรือใช้ Python:
```bash
# Python 3
python3 -m http.server 8000

# เปิดเบราว์เซอร์ที่ http://localhost:8000/chatbot.html
```

#### หรือใช้ Node.js:
```bash
npx serve

# เปิดเบราว์เซอร์ที่ http://localhost:3000/chatbot.html
```

## 📚 เอกสารเพิ่มเติม

- [Google Gemini API Documentation](https://ai.google.dev/docs)
- [Gemini API Quickstart](https://ai.google.dev/tutorials/web_quickstart)
- [API Reference](https://ai.google.dev/api/rest/v1beta/models/generateContent)

## 🎯 Next Steps

1. ✅ สร้าง API Key
2. ✅ ใส่ API Key ลงในโค้ด
3. ✅ ทดสอบ Chatbot
4. 📝 ปรับแต่ง System Prompt ตามต้องการ
5. 🔒 ซ่อน API Key ก่อน Deploy
6. 🚀 Deploy เว็บไซต์

## 💡 Tips

- ใช้ Gemini Pro เพื่อความสมดุลระหว่างคุณภาพและความเร็ว
- Temperature สูง (0.8-1.0) = คำตอบสร้างสรรค์และหลากหลาย
- Temperature ต่ำ (0.0-0.3) = คำตอบที่สม่ำเสมอและน่าเชื่อถือ
- ใช้ System Prompt ที่ชัดเจนเพื่อควบคุมลักษณะการตอบ
