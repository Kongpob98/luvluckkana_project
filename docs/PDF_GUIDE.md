# 📄 PDF to Knowledge Base - วิธีใช้งาน

## 🎯 วิธีแปลง PDF เป็น Knowledge Base สำหรับ AI

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
pip install pdfplumber
```

หรือถ้าใช้ conda:
```bash
conda install -c conda-forge pdfplumber
```

### ขั้นตอนที่ 2: เตรียม PDF

วาง PDF ของคุณใน folder `LUCKKANAPro/` หรือที่ไหนก็ได้

ตัวอย่าง:
```
LUCKKANAPro/
  ├── zodiac_data.pdf        ← PDF ของคุณ
  ├── tools/
  │   └── pdf_to_knowledge.py
```

### ขั้นตอนที่ 3: รัน Script

```bash
cd /Users/kongk/LUCKKANAPro/tools
python pdf_to_knowledge.py ../zodiac_data.pdf
```

หรือ:
```bash
python tools/pdf_to_knowledge.py ชื่อไฟล์.pdf
```

### ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์

Script จะสร้างไฟล์:
1. **`pdf_raw_text.json`** - ข้อความดิบจาก PDF (ตรวจสอบว่าแยกข้อความได้ถูกต้อง)
2. **`js/data/pdf-knowledge.js`** - Knowledge Base พร้อมใช้

### ขั้นตอนที่ 5: เชื่อมต่อกับ Chatbot

1. **เพิ่มใน `chatbot.html`:**
```html
<!-- Knowledge Base -->
<script src="js/data/zodiac-knowledge.js"></script>
<script src="js/data/dream-knowledge.js"></script>
<script src="js/data/fortune-knowledge.js"></script>
<script src="js/data/pdf-knowledge.js"></script>  <!-- เพิ่มบรรทัดนี้ -->
```

2. **อัปเดท `chatbot.js`** - เพิ่มการดึงข้อมูลจาก PDF:
```javascript
// ใน getRelevantKnowledge()
if (typeof getKnowledgeByZodiac !== 'undefined') {
    const pdfData = getKnowledgeByZodiac(zodiacName);
    if (pdfData) {
        knowledge += `\n**จาก PDF:**\n${JSON.stringify(pdfData)}\n`;
    }
}
```

---

## 🔧 ปรับแต่ง Script ตาม PDF ของคุณ

### ถ้า PDF มี Format พิเศษ

แก้ไขฟังก์ชัน `parse_zodiac_content()` ใน `pdf_to_knowledge.py`:

```python
def parse_zodiac_content(text_content):
    knowledge = {}
    
    # ปรับให้ตรงกับ format ของ PDF คุณ
    # ตัวอย่าง: ถ้า PDF แบ่งด้วย "---"
    
    for page_data in text_content:
        content = page_data['content']
        sections = content.split('---')  # แบ่งตามเครื่องหมาย
        
        for section in sections:
            # ดึงข้อมูลตามรูปแบบของคุณ
            pass
    
    return knowledge
```

---

## 📊 ตัวอย่าง PDF Format ที่รองรับ

### Format 1: แบบมีหัวข้อชัดเจน
```
ราศีเมษ
ความรัก: คุณจะพบความรักใหม่...
การงาน: โอกาสดีในการเลื่อนตำแหน่ง...
การเงิน: รายได้เพิ่มขึ้น...

ราศีพฤษภ
ความรัก: ความสัมพันธ์มั่นคง...
```

### Format 2: แบบ JSON ใน PDF
```json
{
  "เมษ": {
    "love": "...",
    "career": "..."
  }
}
```

### Format 3: แบบตาราง
| ราศี | ความรัก | การงาน |
|------|---------|--------|
| เมษ  | ...     | ...    |

---

## 🚀 วิธีอื่นในการใช้ PDF

### 1. **Copy-Paste โดยตรง**
- เปิด PDF
- คัดลอกข้อความ
- วางใน `zodiac-knowledge.js` โดยตรง

### 2. **ใช้ AI ช่วยแปลง**
```bash
# ใช้ ChatGPT หรือ Claude
1. แปลง PDF เป็น text
2. ให้ AI จัด format เป็น JSON
3. คัดลอกมาใส่ใน knowledge.js
```

### 3. **ใช้ Online PDF to Text**
- https://www.ilovepdf.com/pdf_to_text
- แปลง PDF เป็น text
- จัด format เอง

---

## 💡 Tips

### ถ้า PDF มีภาพ/ตาราง
```bash
pip install pdf2image pytesseract
# ใช้ OCR ดึงข้อความจากภาพ
```

### ถ้าต้องการ Auto-parse แบบฉลาด
```bash
pip install openai
# ใช้ GPT-4 Vision อ่าน PDF และจัด format
```

### ถ้า PDF เป็นภาษาไทย
```bash
pip install pythainlp
# ใช้ NLP ช่วยตัดคำและจัดหมวดหมู่
```

---

## 🎯 แนะนำ

**สำหรับ PDF ที่เป็นข้อมูลดวง/โหราศาสตร์:**

1. **อ่าน PDF ด้วยตา** - ดูว่ามี format อย่างไร
2. **Copy ตัวอย่าง 2-3 ย่อหน้า** - ทดสอบ script
3. **ปรับ parse_zodiac_content()** - ให้ตรงกับ format
4. **รันทั้งไฟล์** - แปลงทั้งหมด
5. **ตรวจสอบ + แก้ไข** - ดูว่าถูกต้องไหม

---

## ❓ ต้องการความช่วยเหลือ?

ถ้าคุณมี PDF แล้ว:
1. ส่ง screenshot หน้าหนึ่งมาให้ดู
2. หรือ copy ตัวอย่างข้อความมา
3. ผมจะช่วยเขียน parser ให้เฉพาะ!

**พร้อมลองแปลง PDF แล้วหรือยัง?** 📄➡️💾
